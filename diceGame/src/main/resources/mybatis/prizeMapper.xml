<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="net.netne.api.dao.IPrizeDao">

	<insert id="addPrize" parameterType="Prize">
		insert into prize 
	 		(name,stock,probability,create_time) 
	 	values
	 		(#{name},#{stock},#{probability},now())
	 	<selectKey resultType="int" keyProperty="id" order="AFTER">  
  			SELECT LAST_INSERT_ID() as id
  		</selectKey>  
	</insert>
     
	 <update id="modifyPrize" parameterType="Prize">
	     update prize set 
	    	 name = #{name},stock = #{stock} ,probability=#{probability},photo=#{photoData}
	     where id = #{id};
	 </update>
	 
	 <select id="getPrizeById" resultType="Prize" parameterType="Integer">
		select id,name,probability,stock from prize where id = #{id};
	 </select>
	 
	 <select id="getPrizeList" resultType="Prize" parameterType="map">
	 	select id,name,probability,stock from prize order by id desc limit #{startNum},#{size};
	 </select>
	 
	 <update id="modifyStockPrize" parameterType="map">
	 	update prize set stock = stock - #{stock} where id = #{id}
	 </update>
	
	 <select id="getPrizeCount" resultType="long">
	 	select count(id) from prize;
	 </select>
	 
	 <insert id="addPrizeMember" parameterType="map">
		insert into prize_member
	 		(prize_id,member_id,member_mobile,member_name,prize_name,create_time) 
	 	values
	 		(#{prizeId},#{memberId},#{memberMobile},#{memberName},#{prizeName},now())
	</insert>
	
	<select id="getPrizeMemberList" resultType="PrizeMember" parameterType="map">
	 	select id,prize_id as prizeId,member_id as memberId,
	 	member_mobile as memberMobile,member_name as memberName,
	 	prize_name as prizeName,create_time as date
	 	from prize_member
	 	order by id desc limit #{startNum},#{size};
	 </select>
	
	 <select id="getPrizeMemberCount" resultType="long">
	 	select count(id) from prize_member;
	 </select>
	 
</mapper> 
